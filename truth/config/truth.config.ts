export {
  TRUTH_ALGORITHM_VERSION,
  TRUTH_ANALYSIS_VERSION,
  TRUTH_ENGINE_VERSION,
  TRUTH_OUTPUT_VERSION,
  TRUTH_PIPELINE_VERSION,
  TRUTH_SCORE_VERSION,
  truthConfig,
} from "@/config/truth.config";
export type { TruthConfig } from "@/config/truth.config";
